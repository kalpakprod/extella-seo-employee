diff --git a/src/crawler/playwright-fetcher.ts b/src/crawler/playwright-fetcher.ts
index 50a46d2..d0a13df 100644
--- a/src/crawler/playwright-fetcher.ts
+++ b/src/crawler/playwright-fetcher.ts
@@ -4,6 +4,12 @@ import type { CoreWebVitals } from '../types.js';
 
 let browserPromise: Promise<Browser> | null = null;
 
+async function assertPublicUrl(url: string): Promise<void> {
+  const guardModule = '/app/extella_safe_fetch.mjs';
+  const { resolvePublicAddresses } = await import(guardModule);
+  await resolvePublicAddresses(url);
+}
+
 /**
  * Try to launch browser with given options
  */
@@ -90,10 +96,14 @@ export async function fetchPageWithPlaywright(
   const browser = await initBrowser();
   const page = await browser.newPage();
 
   try {
+    await assertPublicUrl(url);
+    const routeModule = '/app/extella_browser_route.mjs';
+    const { fulfillThroughSafeFetch } = await import(routeModule);
+    await page.route('**/*', (route) => fulfillThroughSafeFetch(route));
+
     const startTime = performance.now();
 
-    // Navigate and wait for load
     const response = await page.goto(url, {
       waitUntil: 'load',
       timeout,
diff --git a/src/rules/links/external-valid.ts b/src/rules/links/external-valid.ts
index 28ac70f..b2d86f3 100644
--- a/src/rules/links/external-valid.ts
+++ b/src/rules/links/external-valid.ts
@@ -1,7 +1,18 @@
 import type { AuditContext } from '../../types.js';
 import { defineRule, pass, warn } from '../define-rule.js';
 import { fetchUrl } from '../../crawler/fetcher.js';
 
+const externalStatusCache = new Map<string, Promise<number>>();
+
+function fetchExternalStatus(href: string): Promise<number> {
+  let pending = externalStatusCache.get(href);
+  if (!pending) {
+    pending = fetchUrl(href);
+    externalStatusCache.set(href, pending);
+  }
+  return pending;
+}
+
 /**
  * Rule: Check that external links are reachable (warn if unreachable)
  */
@@ -28,23 +39,16 @@ export const externalValidRule = defineRule({
     // Check each external link (limit to first 20 to avoid excessive requests)
     const linksToCheck = externalLinks.slice(0, 20);
 
-    for (const link of linksToCheck) {
-      try {
-        const statusCode = await fetchUrl(link.href);
-
-        // Consider link unreachable if 4xx, 5xx, or network error (0)
-        if (statusCode === 0 || statusCode >= 400) {
-          unreachableLinks.push({
-            href: link.href,
-            statusCode,
-          });
-        }
-      } catch {
-        unreachableLinks.push({
-          href: link.href,
-          statusCode: 0,
-        });
-      }
+    for (let index = 0; index < linksToCheck.length; index += 5) {
+      const batch = linksToCheck.slice(index, index + 5);
+      const results = await Promise.all(batch.map(async (link) => {
+        try {
+          return { href: link.href, statusCode: await fetchExternalStatus(link.href) };
+        } catch {
+          return { href: link.href, statusCode: 0 };
+        }
+      }));
+      unreachableLinks.push(...results.filter(({ statusCode }) => statusCode === 0 || statusCode >= 400));
     }
 
     if (unreachableLinks.length > 0) {
